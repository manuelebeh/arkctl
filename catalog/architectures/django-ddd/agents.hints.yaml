recommended_agents:
  - id: architecture-guardian
    reason: Reviews DDD layer boundaries
  - id: django-best-practices
    reason: Django apps, settings, views, auth, security, testing
  - id: django-orm
    reason: Models, migrations, QuerySets, admin, transactions

brief: |
  Follow Django modular DDD.
  New bounded contexts go in domains/{domain}/ with domain, application, infrastructure.
  Domain must not import infrastructure. Cross-domain access via application or shared.
