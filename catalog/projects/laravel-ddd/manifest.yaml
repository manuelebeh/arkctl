schema_version: 1
id: laravel-ddd
name: Laravel app (ddd)
version: 0.1.0
description: Minimal Laravel modular DDD scaffold

implements:
  architecture: laravel-ddd
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: php
  runtime: php
  tags: [laravel, php]
  file_map:
    index: Application/
    test_suffix: Test.php

source:
  root: ./template
