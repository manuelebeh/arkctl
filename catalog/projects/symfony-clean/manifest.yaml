schema_version: 1
id: symfony-clean
name: Symfony app (clean)
version: 0.1.0
description: Minimal Symfony clean-architecture scaffold

implements:
  architecture: symfony-clean
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: php
  runtime: php
  tags: [symfony, php]
  file_map:
    index: Application/
    test_suffix: Test.php

source:
  root: ./template
