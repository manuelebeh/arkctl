schema_version: 1
id: symfony-standard
name: Symfony app (standard)
version: 0.1.0
description: Minimal Symfony Flex scaffold

implements:
  architecture: symfony-standard
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: php
  runtime: php
  tags: [symfony, php]
  file_map:
    index: Controller/
    test_suffix: Test.php

source:
  root: ./template
