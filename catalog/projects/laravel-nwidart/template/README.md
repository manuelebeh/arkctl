# {{project_name}}

Generated with Ark using architecture **laravel-nwidart**.

1. Run `composer install` (enables merge-plugin for `Modules/*/composer.json`).
2. Add modules with `php artisan module:make Name` once Laravel + nwidart are installed.
3. Keep business code under `Modules/{Module}/`.
