schema_version: 1
id: laravel-nwidart
name: Laravel app (nwidart)
version: 0.1.0
description: Minimal Laravel nwidart/laravel-modules scaffold

implements:
  architecture: laravel-nwidart
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: php
  runtime: php
  tags: [laravel, php]
  file_map:
    index: routes/web.php
    test_suffix: Test.php

source:
  root: ./template
