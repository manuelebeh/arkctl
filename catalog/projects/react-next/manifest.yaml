schema_version: 1
id: react-next
name: React / Next app
version: 0.1.0
description: Minimal React/Next feature-first scaffold (stack unlocks React skills)

implements:
  architecture: feature-first
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: typescript
  runtime: node
  tags: [react, next, web, ui, typescript]
  file_map:
    index: index.ts
    test_suffix: .test.ts

source:
  root: ./template
