schema_version: 1
id: django-apps
name: Django app (apps)
version: 0.1.0
description: Minimal Django domain-apps scaffold

implements:
  architecture: django-apps
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [django, python]
  file_map:
    index: models.py
    test_suffix: test_.py

source:
  root: ./template
