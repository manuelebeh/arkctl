# {{project_name}}

Generated with Ark using architecture **laravel-internachi**.

1. Run `composer install` (path repo loads `app-modules/*`).
2. Add modules with `php artisan make:module module-name` once Laravel + internachi/modular are installed.
3. Module directories are kebab-case; namespaces use `Modules\{Name}\`.
