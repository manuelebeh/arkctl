schema_version: 1
id: fastapi-layered
name: FastAPI app (layered)
version: 0.1.0
description: Minimal FastAPI layered scaffold

implements:
  architecture: fastapi-layered
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [fastapi, python]
  file_map:
    index: api/
    test_suffix: test_.py

source:
  root: ./template
