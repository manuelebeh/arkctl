schema_version: 1
id: fastapi-tiangolo
name: FastAPI app (tiangolo)
version: 0.1.0
description: Minimal FastAPI Tiangolo-style scaffold

implements:
  architecture: fastapi-tiangolo
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [fastapi, python]
  file_map:
    index: app/api/
    test_suffix: test_.py

source:
  root: ./template
