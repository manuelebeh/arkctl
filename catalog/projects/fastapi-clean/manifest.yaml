schema_version: 1
id: fastapi-clean
name: FastAPI app (clean)
version: 0.1.0
description: Minimal FastAPI clean-architecture scaffold

implements:
  architecture: fastapi-clean
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: python
  runtime: python
  tags: [fastapi, python]
  file_map:
    index: presentation/
    test_suffix: test_.py

source:
  root: ./template
