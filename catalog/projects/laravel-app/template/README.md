# {{project_name}}

Generated with Ark using architecture **feature-first** (Laravel / PHP stacks).

Feature public APIs stay at `features/<name>/index.ts` so `ark check` matches the current contract. Put Laravel domain code under `features/<name>/domain/` (and HTTP under `api/` when you grow the app).
