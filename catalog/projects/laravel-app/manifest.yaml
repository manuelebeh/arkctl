schema_version: 1
id: laravel-app
name: Laravel app
version: 0.1.0
description: Minimal Laravel-oriented feature-first scaffold (stack unlocks Laravel pack)

implements:
  architecture: feature-first
  architecture_version: ">=0.1.0 <1.0.0"

stack:
  language: php
  runtime: php
  tags: [laravel, php]
  file_map:
    index: index.ts
    test_suffix: .test.ts

source:
  root: ./template
